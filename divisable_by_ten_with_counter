# In this function, i start with a counter.  Then it does a look to check each number if it's divisable by 10, if it's true, then it adds tothe counter.
# at the end it returns the total counter value.  notice the counter is outside the loop
def divisible_by_ten(nums):
  counter = 0
  for num in nums:
    if num % 10 == 0:
      counter += 1
  return counter


#Uncomment the line below when your function is done
print(divisible_by_ten([20, 25, 30, 35, 40]))
